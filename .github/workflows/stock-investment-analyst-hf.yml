name: Deploy Midas to Hugging Face

on:
  push:
    branches: [ main ]
    paths:
      - 'src/stock-investment-analyst/**'
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/reusable-deploy-hf.yml
    with:
      app_path: src/stock-investment-analyst
      hf_space_name: stock-analyst
    secrets: inherit
