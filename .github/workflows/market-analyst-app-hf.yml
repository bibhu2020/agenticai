name: Deploy Agora to Hugging Face

on:
  push:
    branches: [ main ]
    paths:
      - 'src/market-analyst/**'
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/reusable-deploy-hf.yml
    with:
      app_path: src/market-analyst
      hf_space_name: market-analyst
    secrets: inherit
